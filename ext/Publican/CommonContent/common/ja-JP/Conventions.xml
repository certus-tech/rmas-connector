<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
]>
<section>
	<title>表記方法</title>
	 <para>
		本ガイドは特定の単語や語句を強調したり、 記載内容の特定部分に注意を引かせる目的で次のような表記方法を使用しています。
	</para>
	 <para>
		PDF版 および印刷版では、 <ulink url="https://fedorahosted.org/liberation-fonts/">Liberation Fonts</ulink> セットから採用した書体を使用しています。 ご使用のシステムに Liberation Fonts セットがインストールされている場合、 HTML 版でもこのセットが使用されます。 インストールされていない場合は代替として同等の書体が表示されます。 注記: Red Hat Enterprise Linux 5 およびそれ以降のバージョンにはデフォルトで Liberation Fonts セットが収納されます。
	</para>
	 <section>
		<title>印刷における表記方法</title>
		 <para>
			特定の単語や語句に注意を引く目的で 4 種類の表記方法を使用しています。 その表記方法および適用される状況は以下の通りです。
		</para>
		 <para>
			<literal>等幅の太字</literal>
		</para>
		 <para>
			シェルコマンド、ファイル名、パスなどシステムへの入力を強調するために使用しています。またキー配列やキーの組み合わせを強調するのにも使用しています。 例えば、
		</para>
		 <blockquote>
			<para>
				現在作業中のディレクトリ内のファイル <filename>my_next_bestselling_novel</filename> の内容を表示させるには、 シェルプロンプトで <command>cat my_next_bestselling_novel</command> コマンドを入力してから <keycap>Enter</keycap> を押してそのコマンドを実行します。
			</para>

		</blockquote>
		 <para>
			上記にはファイル名、シェルコマンド、キーが含まれています。 すべて等幅の太字で表されているため文中内で見分けやすくなっています。
		</para>
		 <para>
			キーが 1 つの場合と複数のキーの組み合わせになる場合を区別するため、 その組み合わせを構成するキー同士をハイフンでつないでいます。 例えば、
		</para>
		 <blockquote>
			<para>
				<keycap>Enter</keycap> を押してコマンドを実行します。
			</para>
			 <para>
				1 番目の仮想ターミナルに切り替えるは、 <keycombo><keycap>Ctrl</keycap><keycap>Alt</keycap><keycap>F1</keycap></keycombo> を押します。 X-Windows セッションに戻るには、 <keycombo><keycap>Ctrl</keycap><keycap>Alt</keycap><keycap>F7</keycap></keycombo> を押します。
			</para>

		</blockquote>
		 <para>
			最初の段落では押すべき 1 つのキーを特定して強調しています。 次の段落では同時に押すべき 3 つのキーの組み合わせが 2 種類ありそれぞれ強調されています。
		</para>
		 <para>
			ソースコードの説明では 1 段落内で提示されるクラス名、 メソッド、 関数、 変数名、 戻り値を上記のように <literal>等幅の太字</literal> で表示します。 例えば、
		</para>
		 <blockquote>
			<para>
				ファイル関連のクラス群はファイルシステムに対しては <classname>filesystem</classname>、 ファイルには <classname>file</classname>、 ディレクトリには <classname>dir</classname> をそれぞれ含みます。 各クラスは個別に関連する権限セットを持っています。
			</para>

		</blockquote>
		 <para>
			<application>プロポーショナルの太字</application>
		</para>
		 <para>
			アプリケーション名、 ダイアログボックスのテキスト、ラベル付きボタン、 チェックボックスとラジオボタンのラベル、 メニュータイトルとサブメニュータイトルなどシステム上で見られる単語や語句を表します。 例えば、
		</para>
		 <blockquote>
			<para>
				メインメニューバーから <menuchoice><guimenu>システム &#62; 個人設定 &#62; マウス</guimenu></menuchoice> の順で選択し <application>マウスの個人設定</application> を起動します。 <guilabel>ボタン</guilabel> タブ内で <guilabel>左ききのマウス</guilabel> チェックボックスをクリックしてから <guibutton>閉じる</guibutton> をクリックしマウスの主要ボタンを左から右に切り替えます (マウスを左ききの人が使用するのに適した設定にする)。
			</para>
			 <para>
				<application>gedit</application> ファイルに特殊な文字を挿入する場合は、 メインメニューバーから <menuchoice><guimenu>アプリケーション &#62; アクセサリ &#62; 文字マップ</guimenu></menuchoice> の順で選択します。 次に <application>文字マップ</application> メニューバーから <menuchoice><guimenu>検索 &#62; 検索&hellip;</guimenu></menuchoice> と選択して <guilabel>検索</guilabel> フィールド内にその文字名を入力し <guibutton>次</guibutton> をクリックします。 探している文字が <guilabel>文字表</guilabel> 内で強調表示されます。 この強調表示された文字をダブルクリックすると <guilabel>コピーするテキスト</guilabel> フィールド内に置かれるので次に <guibutton>コピー</guibutton> ボタンをクリックします。 ここでドキュメントに戻り <application>gedit</application> メニューバーから <menuchoice><guimenu>編集 &#62; 貼り付け</guimenu></menuchoice> を選択します。
			</para>

		</blockquote>
		 <para>
			上記には、 アプリケーション名、 システム全体のメニュー名と項目、 アプリケーション固有のメニュー名、 GUI インタフェースで見られるボタンやテキストがあります。 すべてプロポーショナルの太字で表示されているため文中内で見分けやすくなっています。
		</para>
		 <para>
			<command><replaceable>等幅の太字で且つ斜体</replaceable></command> または <application><replaceable>プロポーショナルの太字で且つ斜体</replaceable></application>
		</para>
		 <para>
			等幅の太字やプロポーショナルの太字はいずれであっても斜体の場合は置換可能なテキストか変化するテキストを示します。 斜体は記載されている通りには入力しないテキスト、あるいは状況に応じて変化する出力テキストを表します。 例えば、
		</para>
		 <blockquote>
			<para>
				ssh を使用してリモートマシンに接続するには、 シェルプロンプトで <command>ssh <replaceable>username</replaceable>@<replaceable>domain.name</replaceable></command> と入力します。 リモートマシンが <filename>example.com</filename> であり、 そのマシンで使用しているユーザー名が john なら <command>ssh john@example.com</command> と入力します。
			</para>
			 <para>
				<command>mount -o remount <replaceable>file-system</replaceable></command> コマンドは指定したファイルシステムを再マウントします。 例えば、 <filename>/home</filename> ファイルシステムを再マウントするコマンドは <command>mount -o remount /home</command> になります。
			</para>
			 <para>
				現在インストールされているパッケージのバージョンを表示するには、 <command>rpm -q <replaceable>package</replaceable></command> コマンドを使用します。 結果として次を返してきます、 <command><replaceable>package-version-release</replaceable></command>。
			</para>

		</blockquote>
		 <para>
			上記の太字斜体の単語 &mdash; username、 domain.name、 file-system、 package、 version、 release に注目してください。 いずれもコマンドを発行するときに入力するテキスト用のプレースホルダーかシステムにより出力されるテキスト用のプレースホルダーになっています。
		</para>
		 <para>
			タイトル表示のような標準的な使用の他、 斜体は新しい重要な用語が初めて出現する場合にも使用されます。 例えば、
		</para>
		 <blockquote>
			<para>
				Publican は <firstterm>DocBook</firstterm> の発行システムです。
			</para>

		</blockquote>

	</section>
	
	 <section>
		<title>引用における表記方法</title>
		 <para>
			端末の出力とソースコード一覧は、視覚的に周囲の文から区別されています。
		</para>
		 <para>
			端末に送信される出力は <computeroutput>mono-spaced roman</computeroutput> (等幅の Roman) にセットされるので以下のように表示されます。
		</para>
		 
<screen>books        Desktop   documentation  drafts  mss    photos   stuff  svn
books_tests  Desktop1  downloads      images  notes  scripts  svgs</screen>
		 <para>
			ソースコードの一覧も <computeroutput>mono-spaced roman</computeroutput> (等幅の Roman) でセットされますが、以下のように強調表示されます。
		</para>
		 
<programlisting language="Java">package org.jboss.book.jca.ex1;

import javax.naming.InitialContext;

public class ExClient
{
   public static void main(String args[]) 
       throws Exception
   {
      InitialContext iniCtx = new InitialContext();
      Object         ref    = iniCtx.lookup(&#34;EchoBean&#34;);
      EchoHome       home   = (EchoHome) ref;
      Echo           echo   = home.create();

      System.out.println(&#34;Created Echo&#34;);

      System.out.println(&#34;Echo.echo(&#39;Hello&#39;) = &#34; + echo.echo(&#34;Hello&#34;));
   }
}</programlisting>

	</section>
	
	 <section>
		<title>注記および警告</title>
		 <para>
			情報が見過ごされないよう 3 種類の視覚的なスタイルを使用して注意を引いています。
		</para>
		 <note>
			<title>注記</title>
			 <para>
				注記は説明している部分に対するヒントや近道あるいは代替となる手段などになります。注記を無視しても悪影響はありませんが知っておくと便利なコツを見逃すことになるかもしれません。
			</para>

		</note>
		 <important>
			<title>重要</title>
			 <para>
				Important boxes detail things that are easily missed: configuration changes that only apply to the current session, or services that need restarting before an update will apply. Ignoring a box labeled &#39;Important&#39; won&#39;t cause data loss but may cause irritation and frustration.
			</para>

		</important>
		 <warning>
			<title>警告</title>
			 <para>
				警告は無視しないでください。警告を無視するとデータを喪失する可能性が非常に高くなります。
			</para>

		</warning>

	</section>

</section>


